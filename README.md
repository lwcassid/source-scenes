# source-scenes
The SOURCE Interaction Library — The Cave, Burning Man 2026.
`index.html` is the live site (Netlify auto-deploys `main`).
Source lives in `parts/`, assembled by `tools/build.sh`.
**Start here: `CLAUDE.md`** (project context) and
`.claude/skills/scene-craft/SKILL.md` (how scenes are built).
Deep references in `docs/`.
